import styled from "@emotion/styled/macro";

export const Header = styled.header`
  position: fixed;
  z-index: 10;
  width: 100%;
  display: flex;
  padding: 15px 40px;
  background: var(--main-color);
`;
